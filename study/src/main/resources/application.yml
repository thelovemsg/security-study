spring:
  # DB ??
  datasource:
    # ??? H2 DB? ?? URL
    url: jdbc:h2:tcp://localhost/~/security-test
    # ??? ?? ????
    driver-class-name: org.h2.Driver
    # springboot 2.4 ??? username? ? ??????. ??? ??? ?????.
    username: sa
  jpa:
    # JPA? ???? SQL? ? ? ??.
    show-sql: true
    # ??? ?? ???? ??? ?? ??. ?? - create, ??? - none
    # ????? ??? create? ????
    # ???? none ?? ???. create?? ??? ???? ?? ??????.
    hibernate:
      ddl-auto: create
  # ?? ?? ? ?? always
  output:
    ansi:
      enabled: always
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
  main:
    allow-bean-definition-overriding: true

# ???? ??? ?? trace
logging:
  level:
    org.hibernate.type: trace